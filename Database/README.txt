This system uses a Microsoft SQL Server instance (SQL Express) to store data.

Any other database with a connection for .NET may also be used.

For now, very little database functionality is required.  Data is assigned a timestamp as it
is collected / inserted.